## Practice Paper 2
### Questions

**1. Which protocol is primarily utilized to retrieve emails from a designated mail server to a local client?**  
A) HTTP  
B) POP3  
C) SMTP  
D) FTP  

**2. What generally occurs from the user's perspective during a complete DNS resolution failure?**  
A) The website loads purely in HTML without CSS.  
B) The computer entirely loses internet connectivity.  
C) The browser displays a "Server IP address could not be found" error.  
D) The server automatically switches to an insecure HTTP mode.  

**3. In comparison to a Wide Area Network (WAN), a Local Area Network (LAN) is inherently:**  
A) Restricted to a smaller physical, geographic distance.  
B) Much slower due to localized server congestion.  
C) Relies heavily on satellites for core communication.  
D) Only functional over wireline connections, never wireless.  

**4. In a typical Client-Server Web architecture, which entity is responsible for initially broadcasting the HTTP Request?**  
A) The Web Server  
B) The Database  
C) The Proxy Firewall  
D) The Web Browser (Client)  

**5. By default convention, what is the standard network port utilized for unencrypted HTTP communication?**  
A) 443  
B) 21  
C) 80  
D) 25  

**6. Which attribute is strictly required for the `<a>` tag to successfully function as an active hyperlink?**  
A) `link`  
B) `target`  
C) `href`  
D) `src`  

**7. What is the core functionality of the `<meta charset="UTF-8">` HTML tag?**  
A) To link an external font family from Google Fonts.  
B) To specify the character encoding standard for the HTML document.  
C) To initiate a responsive mobile design layout.  
D) To embed invisible metadata specifically for Search Engines (SEO).  

**8. How does a form `<input type="checkbox">` logically differ from an `<input type="radio">` grouping?**  
A) Checkboxes are purely boolean; radios hold text strings.  
B) Radio buttons are invisible by default; checkboxes are visible.  
C) Radio buttons allow multiple selections; checkboxes restrict to a single selection.  
D) Checkboxes allow multiple selections; radio buttons restrict choices to a single selection within a common group.  

**9. Identify the properly nested HTML syntax from the options below:**  
A) `<p><b>Some Text</p></b>`  
B) `<p><b>Some Text</b></p>`  
C) `<p><b>Some Text<b/></p/>`  
D) `<p><b/>Some Text</b/></p>`  

**10. What is the standard HTML tag combination utilized to generate a drop-down list menu?**  
A) `<dropdown>` and `<item>`  
B) `<list>` and `<option>`  
C) `<select>` and `<option>`  
D) `<form>` and `<menu>`  

**11. Which attribute within the `<form>` tag specifies the URL or endpoint where the form data should be submitted?**  
A) `href`  
B) `method`  
C) `src`  
D) `action`  

**12. Interpreting the shorthand CSS rule `margin: 10px 20px 30px 40px;`, what margins are applied to the element?**  
A) Top 10px, Bottom 20px, Left 30px, Right 40px  
B) Top 10px, Right 20px, Bottom 30px, Left 40px  
C) Left 10px, Right 20px, Top 30px, Bottom 40px  
D) Default 10px, Override 40px  

**13. Which value of the CSS `display` property visibly hides an element while also entirely removing it from the document layout flow?**  
A) `visibility: hidden;`  
B) `display: block;`  
C) `opacity: 0;`  
D) `display: none;`  

**14. What is the correct CSS syntax to concurrently apply an identical style to all `<h1>` and `<h2>` tags?**  
A) `h1 h2 { }`  
B) `h1, h2 { }`  
C) `h1 + h2 { }`  
D) `h1 > h2 { }`  

**15. By default, what is the initial `position` property value for all standard HTML elements?**  
A) `relative`  
B) `absolute`  
C) `fixed`  
D) `static`  

**16. Among the following, which is NOT considered a relative CSS length unit?**  
A) `em`  
B) `rem`  
C) `%` (Percentage)  
D) `px` (Pixels)  

**17. Give the HTML snippet `<p style="color:red;" class="blue-text">Hello</p>` and the external CSS rule `.blue-text { color: blue !important; }`, what will be the final rendered text color?**  
A) Red  
B) Blue  
C) Black  
D) Purple (Blend)  

**18. What will be the console output of `console.log("5" == 5);` and `console.log("5" === 5);` in sequence?**  
A) `true`, `true`  
B) `false`, `false`  
C) `false`, `true`  
D) `true`, `false`  

**19. Identify the correct standard syntax for a `for` loop in JavaScript.**  
A) `for (i <= 5; i++)`  
B) `for (let i = 0; i < 5; i++)`  
C) `for i = 1 to 5`  
D) `foreach (i in 5)`  

**20. Consider the code `let arr = [1, 2, , 4]; console.log(arr.length);`. What is the visible output?**  
A) `3`  
B) `undefined`  
C) `Error`  
D) `4`  

**21. How do you properly declare a standard function in JavaScript?**  
A) `function:myFunction()`  
B) `void myFunction()`  
C) `function myFunction()`  
D) `create myFunction()`  

**22. If you assign a value to a completely undeclared variable in non-strict JavaScript mode (e.g., `x = 10;`), what happens?**  
A) The browser immediately crashes.  
B) The code gracefully skips the line.  
C) It automatically becomes a global variable.  
D) It throws an "Undeclared Reference" SyntaxError.  

**23. What will `console.log(undefined + 2);` display in the console?**  
A) `2`  
B) `undefined2`  
C) `NaN` (Not a Number)  
D) `null`  

**24. What does DOM stand for in modern web development?**  
A) Document Overriding Module  
B) Data Object Map  
C) Document Object Model  
D) Display Operating Memory  

**25. An element `<button id="btn" onclick="alert('Hello')">Click</button>` is an implementation example of which event handling strategy?**  
A) Asynchronous Event Delegation  
B) Inline Event Handling  
C) DOM Level 2 Event Listeners  
D) Back-end Scripting  

### Answer Sheet – Paper 2
1. B
2. C
3. A
4. D
5. C
6. C
7. B
8. D
9. B
10. C
11. D
12. B
13. D
14. B
15. D
16. D
17. B
18. D
19. B
20. D
21. C
22. C
23. C
24. C
25. B
